import {
  navbarLinkClass
} from '../../../helpers/navbar-link-class';
import { module, test } from 'qunit';

module('NavbarLinkClassHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = navbarLinkClass(42);
  assert.ok(result);
});
